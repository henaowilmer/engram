# Review Ledger — setup-protocol-flag

Round 1 — judgment-day (jd-judge-a + jd-judge-b, blind parallel) over the PROPOSAL (this repo's 5-phase flow folds design into propose). Pre-implementation review.

| id | lens | severity | file:line | status | summary |
|----|------|----------|-----------|--------|---------|
| JD-001 | judgment-day | CRITICAL | proposal.md:34,78,109-111 (vs plugin/claude-code/scripts/session-start.sh:124,126,133) | verified | Removed the "preserve dynamic import status" claim from Intent/Approach/Success Criteria and explore.md; replaced with the verified fact that session-start.sh:124,126,133 redirects `engram sync --import` output to /dev/null and backgrounds it, so it never reaches hook stdout. Noted the older-plugin-version origin of the prior evidence. |
| JD-002 | judgment-day | CRITICAL | proposal.md (gating range) vs post-compaction.sh:35-69,68,71-81 | verified | Corrected the gating range to `post-compaction.sh:35-67`; the header at line 68 ("CRITICAL INSTRUCTION POST-COMPACTION — follow these steps IN ORDER:") is now specified as staying OUT of the gated block, unconditional alongside the numbered steps (71-81). |
| JD-003 | judgment-day | CRITICAL | proposal.md:34,78 (vs internal/setup/setup.go:217,229) | verified | Replaced all "FIRST ACTION REQUIRED" references with the actual literal header text (post-compaction.sh:68); added a clarifying note that "FIRST ACTION REQUIRED" is Codex/declarative-adapter vocabulary from memoryProtocolMarkdown (setup.go:217,229), out of scope here. |
| JD-004 | judgment-day | CRITICAL | proposal.md Intent (vs internal/mcp/mcp.go:176,241; session-start.sh:27) | verified | Added hook-runtime guard to Approach + Success Criteria: `protocol-mode` returns `slim` only when persisted mode is `slim` AND `engram version` reports ≥1.4.0 at that moment; any failure emits full. Implemented in Go inside the subcommand. Added Risks row replacing the unqualified MCP-delivers-the-prose assumption; added named test (mode=slim + version-check failure → full). |
| JD-005 | judgment-day | CRITICAL | cmd/engram/main.go:2334,662,604-624 | verified | Added requirement to Approach + Tests: writer (cmdSetup) and reader (protocol-mode) must share main()'s resolved cfg/DataDir (main.go:604-624), not a re-derived store.DefaultConfig(); noted cmdSetup is invoked bare at main.go:662 and needs cfg threaded through. Named test: write/read path parity under ENGRAM_DATA_DIR. |
| JD-006 | judgment-day | WARNING | proposal.md Approach (vs cmd/engram/main.go:2338; main_extra_test.go:3749) | verified | Added explicit token-classification table to Approach covering --help/-h/help (any position), --protocol=<v>, --protocol <v> space form, unknown hyphen token (interactive fallback), first/second bare token, and slug-less+flag case. Stated explicitly that the loop REPLACES the old os.Args[2]-only branch. Named tests per row. |
| JD-007 | judgment-day | WARNING | proposal.md:11,109 | verified | Corrected "~10KB" to measured values everywhere: session-start.sh ~2.0KB + post-compaction.sh ~1.6KB (~3.6KB total, ~900 tokens); noted the prior 10KB figure came from an older installed plugin version. |
| JD-008 | judgment-day | WARNING | proposal.md Affected Areas (vs internal/setup/registry_test.go:139-146) | verified | Repointed planned adapter-text assertions to internal/setup/registry_test.go:139-146 (existing generic strings.Contains check) instead of setup_test.go; plan is to strengthen there. |
| JD-009 | judgment-day | WARNING | proposal.md Risks (vs cmd/engram/main.go:667-670,2441) | verified | Added invariant to Approach + Risks: $mode must never be echoed/logged to the hook's own stdout, since an old binary's unrecognized-subcommand path dumps full printUsage() (main.go:667-670,2441) and hook stdout reaches the model. |
| JD-010 | judgment-day | WARNING (theoretical) | proposal.md Approach (vs cmd/engram/main.go:1429-1433) | verified | Adopted the cmdSync loop form (main.go:1429-1433) for --help detection at any position in os.Args[2:], making the "mirrors cmdSync" claim accurate; consistent with the JD-006 table. |
| JD-011 | judgment-day | SUGGESTION | proposal.md Tests | verified | Added named test bullets: corrupted/malformed protocol-mode.json → full; missing file → full; missing slug key → full. |

Round 2 — scoped re-judge: JD-001..JD-006, JD-008, JD-010, JD-011 RESOLVED by both judges; JD-007 and JD-009 completed by the orchestrator (added the ~900-token figure to Intent/Success Criteria/explore intro; added the $mode-echo Risks row and tightened the hyphen-fallback mitigation wording). Terminal state: JUDGMENT APPROVED (proposal).

Implementation round 1 — judgment-day over the uncommitted apply diff (parallel with sdd-verify, which passed 0-critical and confirmed the live-binary probe: setup --help contains --protocol, exit 0, no hang).

| JD-012 | judgment-day | CRITICAL | cmd/engram/main.go:676-688,663-664 (vs internal/version/check.go:47-107) | verified | Added `"protocol-mode"` to the `case "mcp", "serve":` exclusion in `shouldCheckForUpdates` (main.go:682). Regression test: `TestUpdateChecksSkipCriticalStartupCommands` (main_extra_test.go) now asserts `shouldCheckForUpdates(["protocol-mode","claude-code"])` is false. |
| JD-013 | judgment-day | WARNING | internal/setup/protocol.go:47-67 | verified | `WriteProtocolMode` now writes via `os.CreateTemp` in the same dir + `os.Rename` (atomic on POSIX) through the new `writeProtocolModeFileAtomic` helper; a corrupted (unparseable, non-missing) file now returns an error instead of being silently treated as start-fresh, so it is left untouched on disk. Tests: `TestWriteProtocolModeCorruptedFileReturnsErrorAndPreservesFile`, `TestWriteProtocolModeAtomicShapeNeverOpensFinalPathForWrite` (internal/setup/protocol_test.go). |
| JD-014 | judgment-day | WARNING | cmd/engram/main.go:2342-2427 | verified | Residual: the prior fix only forwarded `--protocol` when it appeared BEFORE the unknown hyphen token, since the old loop dispatched to the interactive fallback mid-loop and never read tokens after it (`setup --bogus-flag --protocol=slim` still dropped the mode). Refactored `cmdSetup` to a two-pass, order-independent algorithm: pass 1 only accumulates classification state (helpSeen, protocolFlag/protocolRaw, slug, extraBareSeen, unknownFlagSeen) without dispatching; pass 2 dispatches once after the full scan, in fixed priority order (help > extraBare > unknownFlag > slug > protocol-only > no args). `--protocol` is now parsed regardless of its position relative to an unrecognized flag. Test: `TestCmdSetupUnknownFlagBeforeProtocolStillForwardsMode` (cmd/engram/setup_protocol_test.go), reverse-order counterpart of `TestCmdSetupUnknownFlagFallbackForwardsParsedProtocolMode`. Not marked verified — pending orchestrator verification. |
| JD-015 | judgment-day | WARNING (theoretical) | cmd/engram/main.go:2354-2361 | verified | Space-form `--protocol` now only consumes `args[i+1]` as the value when it does not have a `-` prefix; otherwise treated as dangling (empty value, defaults to full with a warning) and the next token is classified normally. Test: `TestCmdSetupProtocolSpaceFormDoesNotSwallowNextFlag` (cmd/engram/setup_protocol_test.go). |
| JD-016 | judgment-day | SUGGESTION | cmd/engram/setup_protocol_test.go | verified | Added `TestCmdSetupProtocolDanglingFlagDefaultsFullWithWarning`, `TestCmdSetupDuplicateProtocolFlagLastWins`, `TestCmdSetupSlugThenUnknownFlagFallsBackToInteractive`. |

Implementation round 2 — scoped re-judge: JD-012/013/015/016 RESOLVED by both judges; JD-014 residual (order-dependence, judges split on severity, orchestrator sided with judge A) fixed via two-pass order-independent classification and verified directly by the orchestrator (16/16 cmdSetup tests green including both flag orderings; dispatch code-read confirmed; user-approved verification mode per protocol contradiction gate). Terminal state: JUDGMENT APPROVED (implementation).
